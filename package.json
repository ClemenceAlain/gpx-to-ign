{
  "name": "gpx-to-ign",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --build --force && tsc --noEmit -p packages/core/tsconfig.test.json",
    "dev": "npm run dev --workspace @gpx-to-ign/web"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
