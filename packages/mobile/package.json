{
  "name": "@gpx-to-ign/mobile",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "npm run build --workspace @gpx-to-ign/web && cap sync android",
    "apk": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/filesystem": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
